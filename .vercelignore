# Catatan: scripts/ & assets/js/src/ TIDAK di-ignore karena dibutuhkan
# saat "npm run build" di Vercel (obfuscation build-time).
# Akses publik ke path tsupun DIBLOKIR via redirects di vercel.json -> /blocked.

# Build & dependency
node_modules/
# Watermark build cache
assets/img/.wm-cache/
# Secrets & internal
.env
.admin-credentials.txt
.vercel
todo-keamanan-admin.md
# Database schema (struktur DB tidak boleh publik)
supabase/
# QA artifacts
qa-*.png
qa-*.mjs
